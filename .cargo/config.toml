# Force static CRT linkage on Windows to avoid CRT mismatch errors (e.g., MT vs MD) 
# between different C++ dependencies like tokenizers and rocksdb.
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static"]
